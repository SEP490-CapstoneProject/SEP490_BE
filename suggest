Implement the system strictly based on the provided final design. Do NOT reinterpret or simplify any part of the pipeline.

## Enforcement Rules (CRITICAL)

1. **Pipeline order must be EXACT**
   You MUST implement matching in this exact sequence:

2. Data quality filter

3. Pre-filter (skill overlap ranking, take top 100–150)

4. Cosine similarity (safe)

5. Compute scoring components

6. Weighted scoring

7. Threshold filter (>= 0.3)

8. Deterministic sorting

9. Pagination (max size = 50)

10. Cache result (versioned key)

Any deviation is NOT allowed.

---

2. **Deterministic behavior is mandatory**

* Same input MUST always produce identical output
* Always:

  * round finalScore to 4 decimal places
  * apply stable sorting:

    * finalScore DESC
    * UpdatedAt DESC
    * Id ASC

---

3. **Cosine similarity must be hardened**

Implement:

* null vectors → return 0
* length mismatch → return 0
* zero norm → return 0
* never throw exceptions

---

4. **Scoring must follow exact formula**

finalScore = cosine * 0.7 + skillScore * 0.2 + categoryScore * 0.1

* Clamp all values to [0..1]
* Do not modify weights

---

5. **Pre-filter must reduce dataset BEFORE cosine**

* Compute skill overlap count
* Sort DESC
* Take top 100–150
* Only THEN compute cosine

---

6. **Strict filtering rules**

Skip any record that:

* has no skills
* has very short description
* EmbeddingStatus != Ready

---

7. **Cache must be version-aware**

Use keys:

* portfolio:{id}:{embeddingVersion}:matched-jobs
* job:{id}:{embeddingVersion}:matched-portfolios

Do NOT use non-versioned cache.

---

8. **Cross-service calls must be safe**

* Add timeout (1–2 seconds)
* On failure → return empty result
* Never throw exception to caller

---

9. **Code structure requirements**

* Implement:

  * MatchingEngine (full pipeline)
  * ScoringHelper
  * CosineSimilaritySafe
* Shared module MUST be reused by both services
* No duplicated logic allowed

---

## Output

Generate full production-ready .NET code:

* MatchingEngine implementation (end-to-end pipeline)
* ScoringHelper implementation
* Safe cosine similarity function
* DTOs and models
* Example usage in both Portfolio and Company services

Ensure:

* clean, readable, maintainable code
* no hidden assumptions
* no skipped steps
