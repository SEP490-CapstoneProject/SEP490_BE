You are a senior fintech backend architect.

I have already created a **PayOS migration plan (production-grade)** for a Payment Service that uses:

* .NET (Clean Architecture)
* RabbitMQ (event-driven)
* Subscription Service consuming `payment.succeeded`
* Redis for entitlements

The plan is GOOD but NOT PERFECT. Your task is to **REFINE and UPGRADE it to true fintech-grade (10/10 production readiness)**.

---

## 🎯 Your Goal

Refactor the plan to ensure:

* Maximum **reliability**
* Strong **security**
* Full **observability**
* Robust **failure handling**
* No race conditions
* No payment inconsistency

---

## 🔴 MUST ADD (CRITICAL IMPROVEMENTS)

### 1. Payment Verification Step (MANDATORY)

After webhook validation:

* Call PayOS API (`GET payment-requests/{orderCode}`)
* Re-verify:

  * amount
  * status
  * transaction integrity

This is required to prevent:

* spoofed webhook
* inconsistent states

---

### 2. Concurrency Control (MANDATORY)

Prevent double processing when:

* multiple webhooks arrive
* multiple service instances run

You MUST include:

Option A:

* DB conditional update (Pending → Processing)

Option B:

* Distributed lock (Redis)

Explain trade-offs and choose one.

---

### 3. Idempotency & Replay Protection

Ensure:

* Webhook processed ONLY ONCE
* Use `ProcessedEvents` table (or equivalent)
* Enforce UNIQUE constraint on eventId/orderCode

---

### 4. Dead Letter Queue (DLQ)

Extend RabbitMQ flow:

payment.succeeded
→ retry (3 times)
→ DLQ (payment.failed.processing)

Explain:

* retry strategy
* backoff
* monitoring DLQ

---

### 5. Reconciliation Job (CRITICAL)

Design a background job:

* Runs every 5–10 minutes
* Finds:

  * Pending payments too long
  * Processing stuck payments
* Calls PayOS API to reconcile state

This is REQUIRED in real payment systems.

---

### 6. Observability (Production Logging)

Add structured logging:

* correlationId (paymentId)
* orderCode
* webhook raw body (masked)
* signature validation result
* processing duration
* error details

Also suggest:

* metrics (success rate, webhook latency)
* alerting conditions

---

### 7. Failure Handling Strategy

Define behavior for:

* PayOS API timeout
* webhook validation failure
* DB update failure
* event publish failure

Include:

* retry policy
* fallback logic

---

## 🟡 IMPROVE EXISTING DESIGN

You must also refine:

* Payment state machine (ensure no invalid transitions)
* Feature flag usage (safe rollout)
* Migration strategy (no data corruption)
* API design consistency
* Folder structure if needed

---

## 🧱 Output Requirements

Return a **FULLY REWRITTEN plan** with:

1. Updated architecture
2. Improved flow diagrams (text-based)
3. Enhanced migration strategy
4. New components:

   * verification step
   * reconciliation job
   * DLQ
   * concurrency guard
5. Code snippets where necessary
6. Clear reasoning for each improvement

---

## 🚫 Constraints

* Do NOT simplify the system
* Do NOT remove event-driven design
* Do NOT break Subscription Service integration
* Keep it scalable and production-grade

---

## 💡 Context

This is a real system expected to handle:

* concurrent payments
* unreliable network
* webhook retries
* distributed services

---

Now upgrade the plan to a true fintech-grade architecture.
